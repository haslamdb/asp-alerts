"""Action Analytics app configuration."""

from django.apps import AppConfig


class ActionAnalyticsConfig(AppConfig):
    """Configuration for the action analytics app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.action_analytics'
    verbose_name = 'Action Analytics'
